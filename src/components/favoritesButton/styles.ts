import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: { 
        flexDirection: 'row', 
        flex: 1,
        alignItems: 'center'
    },
    text: {
        marginLeft: 5,
        color: '#6f6f6f'
    }
});

export default styles;